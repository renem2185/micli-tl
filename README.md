# micli-tl

## Installation (Temporary!)

``` bash
$ sudo npm install -g git+https://github.com/renem2185/micli-tl.git
```

## Usage

``` bash
## At first, please create the profile with `micli-cfg`
$ micli-tl --name <profile_name>
```

## Note

- Usageの通り単品では動きません
- `-h` オプションでヘルプが表示されます


